{
    "sEcho": {{sEcho}},
    "iTotalRecords": {{iTotalRecords}},
    "iTotalDisplayRecords": {{iTotalDisplayRecords}},
    "aaData":[
    {% post in querySet %}
    [
        
        "{{ post.id }}",
        "{{ post.title }}",
        "{{ post.published_on }}",
	"{{ post.published }}"
        ]
    {% if not forloop.last %}
    ,
    {% endif %}
    {% endfor %}
    ]
}
 

